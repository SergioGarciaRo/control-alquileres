# Copia este archivo como .env en producción y rellena los valores

DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://usuario:password@host:5432/dbname?sslmode=require"

# Genera con: openssl rand -base64 32
NEXTAUTH_SECRET="TU_CLAVE_SECRETA_MUY_LARGA_ALEATORIA"
NEXTAUTH_URL="https://tu-dominio.vercel.app"

# Secreto para el endpoint /api/seed (carga datos demo). Omitir en producción real para desactivarlo.
SEED_SECRET="TU_SEED_SECRET_ALEATORIO"
